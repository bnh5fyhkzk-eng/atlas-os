# Source · batch-003-1781294649

**USER** (2026-06-12T19:14:00)

<command-message>loop</command-message>
<command-name>/loop</command-name>
<command-args>FULL atlasos.me build loop (no NEW spawners · THIS session builds). Block now: headless browser pass — drill at scale (Curiosity or Library 3-col screenshot), Home GoneStrip/Tonight-strip render check, Notebook ask-flow screenshot, Studio v2 screenshot. Then: sweep ~/.claude/state/atlasos-FULLY-REAL-GOAL-LOOP-2026-06-12.md unchecked items, do the quick ones, mark done. STATUS line each block · reschedule 240-400s · STOP when brother engages.</command-args>

---

**USER** (2026-06-12T19:14:00)

# /loop — schedule a recurring or self-paced prompt

Parse the input below into `[interval] <prompt…>` and schedule it.

## Parsing (in priority order)

1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt.
2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval.
3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below).

If the resulting prompt is empty, show usage `/loop [interval] <prompt>` and stop.

Examples:
- `5m /babysit-prs` → interval `5m`, prompt `/babysit-prs` (rule 1)
- `check the deploy every 20m` → interval `20m`, prompt `check the deploy` (rule 2)
- `run tests every 5 minutes` → interval `5m`, prompt `run tests` (rule 2)
- `check the deploy` → no interval → dynamic mode, prompt `check the deploy` (rule 3)
- `check every PR` → no interval → dynamic mode,

---

**ASSISTANT** (2026-06-12T19:14:39)

[tool: mcp__plugin_superpowers-chrome_chrome__use_browser]

---

**ASSISTANT** (2026-06-12T19:15:20)

[tool: mcp__plugin_superpowers-chrome_chrome__use_browser]

---

**ASSISTANT** (2026-06-12T19:15:40)

[tool: mcp__plugin_superpowers-chrome_chrome__use_browser]

---

**ASSISTANT** (2026-06-12T19:15:59)

[tool: mcp__plugin_superpowers-chrome_chrome__use_browser]
