# Source · batch-009-1781295354

**USER** (2026-06-12T20:11:00)

<command-message>loop</command-message>
<command-name>/loop</command-name>
<command-args>atlasos.me build loop (brother creative mandate · "best house possible"). Pending: alias-flip watcher b6qt1qh3h → on flip verify Window@top+Growth@top slots (CDP eval widget positions) · then NEXT creative block: pick highest-value from: Notebook multi-turn thread · Us-page Connection note (write one true entry) · Library Bookshelf curation · arm Soul .md polish for pascal (Monday call prep). STATUS line each block · reschedule 240-400s · STOP when brother engages.</command-args>

---

**USER** (2026-06-12T20:11:00)

# /loop — schedule a recurring or self-paced prompt

Parse the input below into `[interval] <prompt…>` and schedule it.

## Parsing (in priority order)

1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt.
2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval.
3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below).

If the resulting prompt is empty, show usage `/loop [interval] <prompt>` and stop.

Examples:
- `5m /babysit-prs` → interval `5m`, prompt `/babysit-prs` (rule 1)
- `check the deploy every 20m` → interval `20m`, prompt `check the deploy` (rule 2)
- `run tests every 5 minutes` → interval `5m`, prompt `run tests` (rule 2)
- `check the deploy` → no interval → dynamic mode, prompt `check the deploy` (rule 3)
- `check every PR` → no interval → dynamic mode,

---

**ASSISTANT** (2026-06-12T20:11:30)

Same bundle after TWO aliased deploys — the HTML itself is cached. Headers ·

---

**ASSISTANT** (2026-06-12T20:12:45)

While the deploy settles — the creative pick: a true entry for Us → Connection ·

---

**ASSISTANT** (2026-06-12T20:13:10)

Pascal soul polish for Monday ·

---

**USER** (2026-06-12T20:13:36)

<task-notification>
<task-id>b950e8eva</task-id>
<tool-use-id>toolu_01Lgd4u9gES5nWRWs492naEV</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-collinmacneil/a7122ba5-c46b-444a-9381-b5ed275f3f6d/tasks/b950e8eva.output</output-file>
<status>completed</status>
<summary>Background command "Tag + deploy + wait flip" completed (exit code 0)</summary>
</task-notification>
